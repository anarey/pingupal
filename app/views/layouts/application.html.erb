<!DOCTYPE html>

<html>
    <head>
        <title>
            Mi proyecto en Ruby | <%= yield(:title) %>
        </title>
        <%= stylesheet_link_tag    "application", :media => "all" %>
        <%= javascript_include_tag "application" %>
        <%= csrf_meta_tags %>  
    </head>
    <body>
        <p>El título de esta pagina es <%= yield(:title) %></p>
        <%= yield %>
    </body>
</html>
